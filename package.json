{
  "name": "latex-in-desk",
  "version": "1.0.0",
  "description": "A local LaTeX equation editor with PNG and PowerPoint-ready SVG export.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "preview": "node serve.js",
    "check": "node --check main.js && node --check preload.js && node --check renderer.js && node --check serve.js"
  },
  "dependencies": {
    "codemirror": "5.65.16",
    "mathjax": "3.2.2"
  },
  "devDependencies": {
    "electron": "31.7.7"
  }
}
